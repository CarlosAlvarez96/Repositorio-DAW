
-- Ejercicio 1


-- Ejercicio 2

SELECT empleado.nombre , NVL(Jefe. nombre, "SIN JEFE") AS "nombre jefe"
,NVL(SuperJefe.nombre, "SIN JEFE") AS "Jefe del Jefe"
FROM empleado LEFT JOIN empleado AS
Jefe ON (Jefe.id_empleado empleado.id_jefe)
LEFT JOIN empleado AS SuperJefe ON (
SuperJefe.id_empleado-Jefe.id_jefe);